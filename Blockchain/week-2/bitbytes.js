const name = "Netra";

// Bytes array to hex. 
function arrayToHex(byteArray){

}

const byteArray = new Uint8Array([72, 101, 108, 108]); // Corresponds to "Hello".
const hextString = arrayToHex(byteArray);

